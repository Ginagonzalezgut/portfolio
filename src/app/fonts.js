import { Raleway, Sora } from "next/font/google";

export const raleway = Raleway({
  subsets: ["latin"],
  display: "swap",
});

export const sora = Sora({
  subsets: ["latin"],
  display: "swap",
});
